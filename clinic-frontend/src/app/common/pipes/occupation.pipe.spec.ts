import { OccupationPipe } from './occupation.pipe';

describe('OccupationPipe', () => {
  it('create an instance', () => {
    const pipe = new OccupationPipe();
    expect(pipe).toBeTruthy();
  });
});
